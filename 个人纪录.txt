我要做一款能够定时读取屏幕信息，能实时给予评价，并在程序使用者对评价给与语音答复的时候能作出反应的软件。对于该软件，前端以小浮窗的形式呈现（比如一个q版小人），核心是屏幕读取并作出反应的功能，原先的实时语音交互功能转为补充功能，仅在输出屏幕读取后产生的回复信息被软件使用者做出语音回复时才会触发。接下来我会说明我都准备了哪些文档，给我做一下补充

接下来的各项文档仅按基本功能进行区分，具体功能在进行各个文档的单独设计时再细分（比如你提到的性能控制），以下是准备的各项文档：1.总文档，2.API设计文档，3.实时屏幕读取并交互文档，4.语音交互文档，5.技术栈文档，6.前端设计文档，7.测试文档，8.项目进度文档，9.上下文补充说明文档（上传截图时需要附上上下文）。以上是我想到的需要准备的文档，帮我想想还有哪些是比较重要但我没考虑到的文档

实时屏幕读取并交互文档：我预想最多间隔十分钟，最少间隔两分钟进行一次读取，当主程序发生切换时（比如切换游戏，或者切换编程页面时）进行一次读取。读取信息时自动截取全屏，以当前屏幕分辨率为基础获取截屏。我的问题是：我不了解这样对电脑的性能的影响

02_实时屏幕读取模块功能概述：
本模块是桌面互动软件的“视觉感知”基石。它在后台以极低资源占用静默运行，依靠“切屏事件”与“定时兜底”双引擎机制来智能抓取屏幕。为了保护系统性能和控制 API 成本，模块内置了严格的频率控制（默认最高 5 分钟/次，强制最低 15 秒/次防抖）。所有抓取的画面均在内存中直接完成缩放与高压缩比转换（实现 0 磁盘 I/O 的“阅后即焚”），最后将轻量级的图像数据和窗口状态无缝输出给下游状态机，为 AI 提供精准的视觉上下文。


接下来，我们按顺序制作各个文档，我会把我对文档的需求告诉你，你帮我查漏补缺后，我把最终需求告诉你并提示制作文档，然后你按照我的要求制作文档。之后的每一项都按照这个顺序进行

03_视觉上下文与元数据模块功能概述：**
本模块是系统的“上下文解释器”与“注意力向导”。它不仅将底层截图与系统环境（时间、进程、窗口标题）打包在一起，更核心的是引入了 `scenario_rules.json` 场景配置文件。通过识别当前软件（如区分编程IDE、MOBA游戏、RPG游戏等），模块会自动为大模型划分视觉分析的主次优先级（例如编程时主看报错，游戏时主看战绩/队伍状态/剧情），并动态注入专属提示词。同时，它还负责拦截高敏软件界面的截图，确保数据在发送给 AI 前，既具备精准的情景感知能力，又兼顾了用户的绝对隐私。


04_Prompt_Persona.md - [提示词工程与角色设定文档]
[提示词工程与角色设定文档]：结合截图与上下文，定义挂件的性格、语气及视觉分析指令。
04_提示词工程与角色设定模块功能概述：**
本模块为挂件注入“性格”与“逻辑”。它支持前端自定义角色（默认：温柔青年女性），并采用模块化拼装架构将人设、视觉数据和任务指令无缝缝合。其核心亮点是“50轮性格收敛机制”：每 50 次对话自动分析并提取用户偏好与挂件语气，将其固化为长期演化记忆。这使得挂件能够根据 03 模块确定的视觉重心（如报错、战绩）进行主次分明的反应，并随着使用时间的增加，逐渐进化为用户专属、性格稳定的互动伴侣。



05_语音交互模块功能概述：
本模块是挂件发声与聆听的枢纽。它的首要任务是接收大模型生成的文本，并通过“动态声纹映射矩阵”将其转化为流式语音（TTS）。音色会随着前端人设的改变而实时同步切换，确保性格与声音高度统一。其次，模块创新性地设计了“10秒交互窗口”：仅在挂件说完话后的 10 秒内开启麦克风。为了追求极致的响应速度与低成本，语音识别（STT）锁定为高精度的“中英混合”模式，并配合严格的 VAD 降噪算法，彻底屏蔽键盘声等杂音，实现丝滑、无压力的自然对话闭环。


06_交互表现模块功能概述：
本模块为挂件赋予了物理形态和视觉灵魂。为了适应海量且复杂的桌面与游戏环境（从策略塔防到硬核动作大作），模块采用了“鼠标穿透”与“半透明退让”等底层无感化设计，确保挂件能在桌面上长期静默陪伴而不引发任何防误触。在交互时，它通过动态计算字数决定气泡停留时间，并具备智能避让屏幕关键区域的能力。此外，模块将 TTS 音频振幅与 Live2D 口型实时绑定（唇音同步），并结合 AI 情绪触发对应动作，真正将一串串代码转化为一个懂你、陪你、鲜活的“赛博好友”。



**07_API 设计模块功能概述：**
本模块是软件与 AI 大脑连接的通信枢纽。它采用稳定可靠的“整包返回”机制，将本地的截图和提示词安全地送达云端或本地大模型。为了保障极佳的用户体验，模块内置了“密钥热重载”功能（前端改 Key 瞬间生效，无需重启），并设计了强大的多模型路由与容错机制。当遭遇网络断开或 API 欠费时，它能自动切换备用本地模型，或拦截死板的代码错误，转而让挂件以拟人化的口吻提醒用户检查设置，确保软件永不崩溃、陪伴始终在线。


08_核心控制流与状态机模块功能概述：
本模块是整个软件的“大脑交通指挥中心”，负责统筹视觉抓取、AI 请求、语音播报和麦克风监听等状态的无缝互斥与流转。为了实现“绝对不干扰正常使用”的陪伴体验，模块量身定制了三大核心规则：1. 3秒切屏免疫：快速切屏（如双开游戏或切歌）不会触发截图，只有停留超过 3 秒才被视为有效场景；2. 活跃期全局冻结：当挂件处于思考、说话或 10 秒倾听状态时，自动暂停后台的所有定时截图任务，保证单次交互的专注与完整；3. 摇晃鼠标闭嘴：赋予用户最高优先级的物理控制权，只要在挂件上方快速晃动鼠标，即可强制中断其所有动作与发声，瞬间恢复休眠。该模块确保了挂件在复杂、高频的桌面操作中始终进退有度、逻辑清晰。


09_前端设计模块功能概述：
本模块定义了用户与“赛博好友”交互的视觉与操作界面。核心特色是“AI 动态生成 Q 版形象”：初次启动时，系统会根据用户喜好由 AI 绘制专属的 Q 版角色及 5 种基础差分表情（开心、思考、说话等），并通过轻量级的代码形变实现呼吸感与口型同步。桌面挂件采用无边框透明渲染，支持 Alt 键防误触交互与智能气泡避让。此外，模块设计了完备的设置面板，允许用户热重载 API 密钥、重塑角色形象，并能自由调节 02 模块的“最大静默截屏间隔”（最低 15 秒限制）以及麦克风降噪灵敏度，实现了高度的定制化与操作便利性。


10_记忆与数据生命周期模块功能概述：
本模块为软件构建了安全、高效且高度可玩的记忆管理系统。它采用三层记忆架构：截屏与录音等高敏数据作为“瞬时工作记忆”仅在内存流转，阅后即焚，绝不落盘；日常对话作为“短期记忆”，受限于“7天或1000条”的双重清理规则，且每次与AI交互仅携带最近5条记录以节省成本；而由AI提炼的核心羁绊则作为“长期记忆”持久化保存。此外，模块创新性地加入了“多伴侣存档系统”，允许用户创建、切换或彻底销毁不同的赛博好友（存档间数据绝对隔离）。最后，所有准备存入本地的文本都必须经过隐私清洗网关的脱敏打码，从物理层面上捍卫用户的数据安全。


**11_技术栈文档功能概述：**
本模块为“赛博好友”奠定了跨界且高效的底层基石。前端大胆采用 `PyWebView` 结合 `HTML/CSS` 渲染，轻松实现毛玻璃气泡与 Q 版角色的呼吸浮动动画；后端则以 `Python` 为主脑，搭配毫秒级截图神器 `mss` 与异步网络库 `httpx`，确保游戏运行的绝对流畅。在语音方面，直接在本地运行 `FunASR` 实现零成本且绝对隐私的语音转写，并调用微软 `edge-tts` 提供免费且高品质的自然女声。数据库则精简为 Python 自带的 `SQLite3` 和 `JSON`。这套高性价比、易上手的全栈方案完美契合 AI 辅助开发的模式，只需极少量的环境配置，即可将这套系统流畅跑在现代硬件上。



12_测试文档模块功能概述：
本模块为“赛博好友”设定了严格的“出厂体检”标准，旨在确保软件的绝对稳定与无感陪伴。测试方案聚焦四大极端场景：1. 性能测试：在高负载 3A 游戏下测试截图机制，确保不掉帧、不抢占算力，且鼠标点击 100% 穿透防误触；2. 状态机测试：通过疯狂切屏与快速晃动鼠标，验证系统的 3 秒免疫规则和强制闭嘴功能是否灵敏，确保逻辑不卡死；3. 音频测试：模拟剧烈敲击机械键盘的噪音，验证 VAD 能否精准过滤杂音，避免麦克风长开；4. 断网测试：模拟突然断网或 API 欠费，确保软件能优雅地兜底报错并回落至休眠，坚守“永不崩溃闪退”的底线。



13_项目进度模块功能概述：
本模块为“AI 辅助编程”量身定制了五大敏捷开发里程碑，指导人类产品经理如何科学地向 AI 助手下达代码生成指令。进度从底层的“透明窗口载体”搭起（Phase 1），依次接入“截图与 API 通信”（Phase 2）、“语音听写与记忆管理”（Phase 3），然后在“状态机与视觉同步”中完成全系统的融合（Phase 4），最后通过严格的“暴力压测”走向封包发布（Phase 5）。这种“搭积木”式的排期确保了复杂项目在 AI 协助下不会出现逻辑崩塌，极大提高了个人独立开发的成功率。




**14_软件打包与发布模块功能概述：**
本模块为“赛博好友”的最终交付制定了标准。为避免启动缓慢和内存崩溃，放弃单文件打包，采用 `PyInstaller` 的单文件夹（绿色版）策略。核心技术点在于强制引入 `sys._MEIPASS` 动态路径解析机制，确保打包后的程序仍能准确定位前端 HTML 和角色图片。同时，明确要求将 API 密钥和数据库等配置文件暴露在执行文件外部，方便朋友自行配置。最后，针对大体积的本地语音模型及杀软误报问题，制定了清晰的依赖剥离与用户引导策略，确保软件在陌生电脑上也能顺利安家。



15_Context_Notes.md (AI 辅助开发准则与全局备注)
当你把这份规则贴给 Gemini 的那一刻起，它就不再是一个只会盲目生成代码的聊天机器人，而是变成了完全遵从你这个“架构师哥哥”指令的高级打工人。